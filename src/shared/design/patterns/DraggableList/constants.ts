// Hold this long before a press turns into a drag, so taps/scroll still work.
export const LONG_PRESS_MS = 220

// How fast displaced rows slide to make room for the dragged one.
export const SHIFT_DURATION_MS = 150
