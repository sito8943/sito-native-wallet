import { spacing } from "#design/foundations"

export const ICON_SIZE = spacing[4]

export const LIST_MAX_HEIGHT = spacing[24] * 3

// No debounce by default; set a value when options come from an async source.
export const DEFAULT_DEBOUNCE_MS = 0
