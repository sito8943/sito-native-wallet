// Custom fonts loaded at startup (see the root layout's useFonts). Only the
// brand wordmark uses Poppins; the rest of the UI keeps the system font, which
// reads native on each platform and avoids bundling a full type system.
export const FONT_FAMILY = {
  POPPINS_BOLD: "Poppins_700Bold",
} as const
