// Persisted flag that gates first-run onboarding. Bumping the suffix re-runs
// onboarding for everyone (e.g. when a future wizard adds mandatory steps).
export const ONBOARDING_STORAGE_KEY = "onboarding.completed.v1"
