export const DASHBOARD_CONFIG_ENDPOINT = "/user-dashboard-config"

// One page big enough to hold every card a user realistically has; the backend
// defaults to a small page, so we ask for a generous cap instead of paging.
export const DASHBOARD_CONFIG_PULL_PAGE_SIZE = 500
