// Bounded height so the list scrolls inside the (content-sized) bottom sheet.
export const BREAKDOWN_LIST_MAX_HEIGHT = 400

// Height of the per-category proportion bar.
export const BREAKDOWN_BAR_HEIGHT = 6
