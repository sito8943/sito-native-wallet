// Cap the list — it's a "recent" peek, not the full transactions screen.
export const RECENT_LIMIT = 50

// Bounded height so the list scrolls inside the (content-sized) bottom sheet.
export const RECENT_LIST_MAX_HEIGHT = 360
