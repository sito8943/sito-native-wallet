import { type SubscriptionProvider } from "./SubscriptionProvider"

// No demo seed: providers start empty. Users add them from the provider prefab
// picker (or manually).
export const INITIAL_SUBSCRIPTION_PROVIDERS: SubscriptionProvider[] = []
